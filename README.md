# ems-objects-k8s-job
This is a sample which will help you create TIBCO EMS objects (queues, topics, bridges, factories, etc) using Kubernetes jobs where the commands to create these objects are defined in a files which are loaded via a config map.
